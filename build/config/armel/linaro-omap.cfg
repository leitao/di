MEDIUM_SUPPORTED = netboot

# The version of the kernel to use.
KERNELVERSION := 3.0.0-1007-linaro-omap
# we use non-versioned filenames in the linaro-omap kernel udeb
KERNELNAME = vmlinuz
VERSIONED_SYSTEM_MAP =
