MEDIUM_SUPPORTED = netboot cdrom

# The version of the kernel to use.
KERNELVERSION := $(KERNELVERSION)-generic-lpae
# we use non-versioned filenames in the generic kernel udeb
KERNELNAME = vmlinuz
VERSIONED_SYSTEM_MAP =
