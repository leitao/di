MEDIA_TYPE = netboot image

TARGET = $(INITRD) $(KERNEL)
EXTRANAME = $(MEDIUM)/

MANIFEST-BOOT = "tftp boot image for e500mc PowerPC cpus"
MANIFEST-INITRD = "initrd for e500mc PowerPC cpus"
MANIFEST-KERNEL = "kernel for e500mc PowerPC cpus"
